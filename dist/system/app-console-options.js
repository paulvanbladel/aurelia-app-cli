'use strict';

System.register([], function (_export, _context) {
  "use strict";

  var appConsoleOptions;
  return {
    setters: [],
    execute: function () {
      _export('appConsoleOptions', appConsoleOptions = {
        commands: {},
        option1: 'dag jos',
        option2: 1000
      });

      _export('appConsoleOptions', appConsoleOptions);
    }
  };
});